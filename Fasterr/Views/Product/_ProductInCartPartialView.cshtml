@model ProductAllViewModel

@{
    double rating = 0.0;
    if (Model.Rating > 0)
    {
        rating = (double)Model.Rating / (double)Model.RatingCount;
    }
}

<style>

    .cardLink {
        opacity: 1;
        transition: opacity 0.5s;
    }

        .cardLink:hover {
            opacity: 0.65;
        }
</style>

<a asp-area="" asp-controller="Product" asp-action="Info" asp-route-id="@Model.Id" class="cardLink" style="text-decoration: none; color: inherit;">
    <div class="card mb-3" >
        <div class="d-flex g-0">
            <div class="">
                <img src="@Model.ImageURL" class="img-fluid rounded-start" alt="..." style="height: 148px;">
            </div>
            <div class="" style="background-color: #E8E8E8; width: 700px;">
                <div class="card-body">
                    <h5 class="card-title">@Model.Name</h5>
                    <p class="card-text"><b>@Model.Price лв.</b></p>
                </div>
            </div>
        </div>
    </div>
</a>